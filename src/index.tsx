import React from 'react';
import { render } from 'react-dom';

function App() {
    return (
        <div>
            <h2>Forwardslash Development React TS Application</h2>
        </div>
    );
}
render(<App />, document.getElementById('root'));
